// Step 1: Store your birth year in a variable
let birthYear = 1995; // change this to your own birth year

// Step 2: Store a future year in a variable
let futureYear = 2035; // change this to any future year you want

// Step 3: Calculate the two possible ages
let ageIfBirthdayPassed = futureYear - birthYear;
let ageIfBirthdayNotPassed = ageIfBirthdayPassed - 1;

// Step 4: Display the result
console.log("I will be " + ageIfBirthdayNotPassed + " or " + ageIfBirthdayPassed + " in " + futureYear);

